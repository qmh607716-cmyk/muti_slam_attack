lio_sam:

  # Topics — matched to jackal.bag (Livox AVIA, /points_raw, /imu_correct)
  pointCloudTopic: "/points_raw"
  imuTopic: "/imu_correct"
  odomTopic: "odometry/imu"
  gpsTopic: "odometry/gpsz"      # Not present in jackal.bag; GPS not used

  # Frames
  lidarFrame: "base_link"
  baselinkFrame: "base_link"
  odometryFrame: "odom"
  mapFrame: "map"

  # GPS Settings — disabled (jackal.bag has /gps/fix but no odometry/gpsz)
  useImuHeadingInitialization: false
  useGpsElevation: false
  gpsCovThreshold: 2.0
  poseCovThreshold: 25.0

  # Export settings
  savePCD: false
  savePCDDirectory: "/Downloads/LOAM/"

  # Sensor Settings — jackal.bag uses Livox non-repeating scan (same as handheld.bag)
  # N_SCAN=16 matches what slamspoof used for LVI-SAM BiSMVS generation
  sensor: livox
  N_SCAN: 16
  Horizon_SCAN: 4000
  timeField: "time"
  downsampleRate: 1
  lidarMinRange: 1.0
  lidarMaxRange: 1000.0

  # IMU Settings — same as LVI-SAM
  imuAccNoise: 3.9939570888238808e-03
  imuGyrNoise: 1.5636343949698187e-03
  imuAccBiasN: 6.4356659353532566e-05
  imuGyrBiasN: 3.5640318696367613e-05
  imuGravity: 9.80511
  imuRPYWeight: 0.01

  # Extrinsics: T_lb (lidar -> imu) — identical to LVI-SAM config
  extrinsicTrans: [0.0, 0.0, 0.0]
  extrinsicRot: [-1, 0, 0,
                 0, 1, 0,
                 0, 0, -1]
  extrinsicRPY: [0, -1, 0,
                  1, 0, 0,
                  0, 0, 1]

  # LOAM feature threshold
  edgeThreshold: 1.0
  surfThreshold: 0.1
  edgeFeatureMinValidNum: 10
  surfFeatureMinValidNum: 100

  # voxel filter params
  odometrySurfLeafSize: 0.4
  mappingCornerLeafSize: 0.2
  mappingSurfLeafSize: 0.4

  # robot motion constraint
  z_tollerance: 1000
  rotation_tollerance: 1000

  # CPU Params
  numberOfCores: 4
  mappingProcessInterval: 0.15

  # Surrounding map
  surroundingkeyframeAddingDistThreshold: 1.0
  surroundingkeyframeAddingAngleThreshold: 0.2
  surroundingKeyframeDensity: 2.0
  surroundingKeyframeSearchRadius: 50.0

  # Loop closure — ICP scan-to-map, critical for jackal's loop closure
  loopClosureEnableFlag: true
  loopClosureFrequency: 1.0
  surroundingKeyframeSize: 50
  historyKeyframeSearchRadius: 15.0
  historyKeyframeSearchTimeDiff: 30.0
  historyKeyframeSearchNum: 25
  historyKeyframeFitnessScore: 0.3

  # Visualization
  globalMapVisualizationSearchRadius: 1000.0
  globalMapVisualizationPoseDensity: 10.0
  globalMapVisualizationLeafSize: 1.0
